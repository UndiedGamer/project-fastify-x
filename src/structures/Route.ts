import { AliasPiece, AliasPieceOptions, PieceContext } from "@sapphire/pieces";

export class Route extends AliasPiece {
	public constructor(ctx: PieceContext, options: AliasPieceOptions) {
		super(ctx, options);
	}
}